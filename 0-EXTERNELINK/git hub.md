### Commands:

```
git add --a      (wenn neue dateien oder ordner erstell wurden fügt das die datein hinzu)

git commit -m "hier reinschreiben was geändert wurde" ("" kommt ein kommentar; kann auch leer gelassen werden )

git push --set-upstream origin main ( pushed es ins repo in den richtigen branche wird nur dann gebraucht wenn es probleme mit dem main branch gibt)

git push (pusht es online ins repo)

git branch -M main

git remote add origin https://github.com/RamSkirata/Adventuria.git

git push -u origin main

```

git init
git branch -M main
git remote add origin https://github.com/RamSkirata/Adventuria.git
git add .
git commit -m "Initialer Commit ohne PDFs"
git push -u origin main